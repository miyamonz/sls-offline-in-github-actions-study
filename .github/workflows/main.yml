name: run serverless

on:
  push: 
    branches: main

jobs:
  run-serverless-offline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: npm install
        run: npm install

      - name: start serverless offline
        run: npm run dev
